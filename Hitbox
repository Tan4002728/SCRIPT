-- ===== WAIT GAME LOAD + VERSION INTRO =====
do
	-- Đợi game load xong
	wait(2)

	local Players = game:GetService("Players")
	local TweenService = game:GetService("TweenService")

	local player = Players.LocalPlayer
	if not player then return end
	player:WaitForChild("PlayerGui")

	-- GUI
	local gui = Instance.new("ScreenGui")
	gui.Name = "VersionIntroGui"
	gui.IgnoreGuiInset = true
	gui.ResetOnSpawn = false
	gui.Parent = player.PlayerGui

	local text = Instance.new("TextLabel")
	text.Parent = gui
	text.AnchorPoint = Vector2.new(0.5, 0.5)
	text.Position = UDim2.new(0.5, 0, 0.5, 0)
	text.Size = UDim2.new(0, 400, 0, 120)
	text.BackgroundTransparency = 1
	text.Text = "V2.2.0"
	text.Font = Enum.Font.GothamBold
	text.TextColor3 = Color3.fromRGB(255, 255, 255)
	text.TextTransparency = 1
	text.TextStrokeTransparency = 0.75
	text.TextXAlignment = Enum.TextXAlignment.Center
	text.TextYAlignment = Enum.TextYAlignment.Center
	text.TextSize = 16

	-- Tween phóng to + hiện
	local grow = TweenService:Create(
		text,
		TweenInfo.new(0.6, Enum.EasingStyle.Sine, Enum.EasingDirection.Out),
		{
			TextSize = 50,
			TextTransparency = 0.2
		}
	)

	-- Tween mờ dần
	local fade = TweenService:Create(
		text,
		TweenInfo.new(0.6, Enum.EasingStyle.Sine, Enum.EasingDirection.In),
		{
			TextTransparency = 1
		}
	)

	grow:Play()
	grow.Completed:Wait()

	task.wait(3)

	fade:Play()
	fade.Completed:Wait()

	gui:Destroy()
end
wait(1)

local Players = game:GetService('Players')
local TweenService = game:GetService('TweenService')
local LocalPlayer = Players.LocalPlayer

-- Tạo ScreenGui chính
local ScreenGui = Instance.new('ScreenGui')
ScreenGui.Parent = LocalPlayer:FindFirstChildOfClass('PlayerGui')
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.IgnoreGuiInset = true

-- Hàm tạo màn hình bypass key
local function createBypassScreen(text, duration)
    -- Frame nền
    local Container = Instance.new('Frame')
    Container.Parent = ScreenGui
    Container.Size = UDim2.new(0, 250, 0, 100)
    Container.Position = UDim2.new(0.5, -125, 0.5, -50)
    Container.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    Container.BorderSizePixel = 0
    Container.ZIndex = 10
    Container.BackgroundTransparency = 1
    
    local ContainerCorner = Instance.new('UICorner')
    ContainerCorner.CornerRadius = UDim.new(0, 15)
    ContainerCorner.Parent = Container
    
    -- Text label
    local TextLabel = Instance.new('TextLabel')
    TextLabel.Parent = Container
    TextLabel.Size = UDim2.new(1, -20, 1, -20)
    TextLabel.Position = UDim2.new(0, 10, 0, 10)
    TextLabel.BackgroundTransparency = 1
    TextLabel.Text = text
    TextLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    TextLabel.Font = Enum.Font.GothamBold
    TextLabel.TextSize = 20
    TextLabel.TextTransparency = 1
    TextLabel.ZIndex = 11
    TextLabel.TextWrapped = true
    
    -- Fade in
    local fadeInContainer = TweenService:Create(
        Container,
        TweenInfo.new(0.3, Enum.EasingStyle.Sine, Enum.EasingDirection.Out),
        {BackgroundTransparency = 0.1}
    )
    local fadeInText = TweenService:Create(
        TextLabel,
        TweenInfo.new(0.3, Enum.EasingStyle.Sine, Enum.EasingDirection.Out),
        {TextTransparency = 0}
    )
    
    fadeInContainer:Play()
    fadeInText:Play()
    
    -- Tự động ẩn
    task.delay(duration - 0.3, function()
        local fadeOutContainer = TweenService:Create(
            Container,
            TweenInfo.new(0.3, Enum.EasingStyle.Sine, Enum.EasingDirection.In),
            {BackgroundTransparency = 1}
        )
        local fadeOutText = TweenService:Create(
            TextLabel,
            TweenInfo.new(0.3, Enum.EasingStyle.Sine, Enum.EasingDirection.In),
            {TextTransparency = 1}
        )
        
        fadeOutContainer:Play()
        fadeOutText:Play()
        
        fadeOutContainer.Completed:Connect(function()
            Container:Destroy()
        end)
    end)
    
    return Container
end

-- Hiển thị màn hình bypass key
createBypassScreen("Bypass Key By Gr0k3_script", 3)

task.wait(3)

-- Chạy script chính
loadstring(game:HttpGet('https://raw.githubusercontent.com/RedJDarks/Mani/refs/heads/main/Ywyir'))()
